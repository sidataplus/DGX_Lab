# Functional no-build prototype

Run `python3 serve.py`. The prototype is local-only, dependency-free, and never evaluates or executes learner commands. It demonstrates guided allocation, sandbox batch submission/contention, time acceleration, queue/resource views, and deterministic knowledge scoring.

This JavaScript prototype is a product/interaction aid; the Rust/WASM worker remains the intended authoritative implementation.
